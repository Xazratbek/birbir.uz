TZ (Pending)

1. Seller profile API
- `GET /sellers/{user_id}/`
- `PATCH /seller-profile/me/`

Serializer:
- `SellerProfileSerializer`
  - `user_id`
  - `display_name`
  - `avatar`
  - `bio`
  - `is_verified`
  - `is_store`
  - `telegram_username`
  - `phone_visible`
  - `reply_time_minutes`
  - `active_listing_count`
  - `followers_count`

2. Seller follow API
- `POST /sellers/{id}/follow/`
- `DELETE /sellers/{id}/follow/`

Response:
- `seller_id`
- `is_following`
- `followers_count`

3. Listing report API
- `POST /listings/{id}/report/`
- `GET /my/reports/` (admin/moderation keyin)

Serializer:
- `ListingReportCreateSerializer`
  - `reason`
  - `comment`

4. Promotion API
- `GET /promotion-types/`
- `POST /listings/{id}/promotions/`
- `GET /listings/{id}/promotions/`

Serializer:
- `ListingPromotionSerializer`
  - `id`
  - `promotion_type`
  - `starts_at`
  - `ends_at`
  - `is_active`

5. Favorite API
- `POST /listings/{id}/favorite/`
- `DELETE /listings/{id}/favorite/`
- `GET /favorites/`

Serializer:
- `FavoriteListSerializer`
  - `id`
  - `listing`

6. User verify API
- `POST /users/me/verify/complete/`

Logika:
- MyID tasdiq muvaffaqiyatli bo'lsa `user.is_verified = True`
- `verified user` badge seller serializerlarda qaytadi
